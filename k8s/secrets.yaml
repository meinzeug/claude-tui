# NOTE: This is a template file. DO NOT commit actual secrets to version control.
# Use kubectl create secret or external secret management systems.

apiVersion: v1
kind: Secret
metadata:
  name: claude-tiu-secrets
  namespace: claude-tiu-production
  labels:
    app.kubernetes.io/name: claude-tiu
    app.kubernetes.io/instance: production
type: Opaque
stringData:
  # Claude API Configuration
  claude-api-key: "YOUR_CLAUDE_API_KEY_HERE"
  
  # Database Configuration
  database-url: "postgresql://claudetiu:YOUR_DB_PASSWORD@claude-tiu-postgres:5432/claudetiu"
  
  # Redis Configuration (optional)
  redis-url: "redis://claude-tiu-redis:6379/0"
  
  # JWT Secret for session management
  jwt-secret: "YOUR_JWT_SECRET_HERE"
  
  # Encryption key for sensitive data
  encryption-key: "YOUR_ENCRYPTION_KEY_HERE"
  
  # Monitoring and alerting
  grafana-admin-password: "YOUR_GRAFANA_PASSWORD"
  prometheus-admin-password: "YOUR_PROMETHEUS_PASSWORD"
  
  # External service tokens
  slack-webhook-url: "YOUR_SLACK_WEBHOOK_URL"
  datadog-api-key: "YOUR_DATADOG_API_KEY"
  
# Example of using external secret operator instead:
---
# apiVersion: external-secrets.io/v1beta1
# kind: SecretStore
# metadata:
#   name: vault-backend
#   namespace: claude-tiu-production
# spec:
#   provider:
#     vault:
#       server: "https://vault.example.com"
#       path: "secret"
#       version: "v2"
#       auth:
#         kubernetes:
#           mountPath: "kubernetes"
#           role: "claude-tiu"
#           serviceAccountRef:
#             name: "claude-tiu"
# ---
# apiVersion: external-secrets.io/v1beta1
# kind: ExternalSecret
# metadata:
#   name: claude-tiu-secrets
#   namespace: claude-tiu-production
# spec:
#   refreshInterval: 15s
#   secretStoreRef:
#     name: vault-backend
#     kind: SecretStore
#   target:
#     name: claude-tiu-secrets
#     creationPolicy: Owner
#   data:
#   - secretKey: claude-api-key
#     remoteRef:
#       key: claude-tiu/production
#       property: claude_api_key
#   - secretKey: database-url
#     remoteRef:
#       key: claude-tiu/production
#       property: database_url